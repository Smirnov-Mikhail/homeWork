// Task 2 (hw5)
//Смирнов Михаил Александрович
#include "stdafx.h"
#include <iostream>

using namespace std;

//#include "list.h"

int main()
{
	//List *list = create();
	
	/*int choice = 1;
	while (choice)
	{
		switch(choice)
		{
		case 1://добавить
			/*cout << "Введите элемент, который хотите внести в список:" << endl;
			int element = 0;
			cin >> element;
			if (list->head == nullptr || element <= list->head->value)
				insertToHead(list, element);
			for (Position i = list->head->next; i != nullptr; i->next)
			{
				if (element <= i->value)
					insert(list, element, i);
			}
			break;
		case 2://удалить
			break;
		case 3://вывести
			break;
			//print(list);
		}
	}*/
	system("pause");
	return 0;
}